# 2026-04-24 工作日志

## 预测功能实现
- 新增 `backend/app/models/prediction.py`：PredictionResult + PredictionPoint 数据模型
- 新增 `backend/app/schemas/prediction.py`：请求/响应 Pydantic Schema
- 新增 `backend/app/services/prediction.py`：核心预测逻辑
  - 加载股票数据 + 训练好的 LSTM 模型
  - 生成全量预测并反标准化为真实价格
  - 应用交易策略生成信号
  - 计算次日预测结果（价格+方向+策略信号）
  - 保存预测数据点到数据库
  - 计算精度指标（MAE/RMSE/方向准确率）
  - 支持后续匹配实际收盘价
- 新增 `backend/app/api/prediction.py`：5个API路由
- 更新 `main.py`：注册 prediction_router
- 更新 `models/__init__.py`、`user.py`、`training.py`：添加关联
- 修复 `database.py` 循环导入：eager import → lazy `ensure_models_registered()` 函数
- 新增 `backend/migrate_add_predictions.py`：迁移脚本（已执行成功）
- 更新 `init_db.py`：添加新模型导入
